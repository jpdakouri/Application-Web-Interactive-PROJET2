import { ClipboardDeleteCommand } from './clipboard-delete-command';

describe('ClipboardDeleteCommand', () => {
  it('should create an instance', () => {
    expect(new ClipboardDeleteCommand()).toBeTruthy();
  });
});

import { ClipboardCopyCommand } from './clipboard-copy-command';

describe('ClipboardCopyCommand', () => {
  it('should create an instance', () => {
    expect(new ClipboardCopyCommand()).toBeTruthy();
  });
});

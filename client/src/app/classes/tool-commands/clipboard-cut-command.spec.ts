import { ClipboardCutCommand } from './clipboard-cut-command';

describe('ClipboardCutCommand', () => {
  it('should create an instance', () => {
    expect(new ClipboardCutCommand()).toBeTruthy();
  });
});

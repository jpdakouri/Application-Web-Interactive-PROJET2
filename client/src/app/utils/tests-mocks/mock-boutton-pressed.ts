export enum MouseButton {
    Left = 0,
    Middle = 1,
    Right = 2,
    Back = 3,
    Forward = 4,
}

export enum KeyboardButton {
    Shift = 'Shift',
    Escape = 'Escape',
    Backspace = 'Backspace',
}

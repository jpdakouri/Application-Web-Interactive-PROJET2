import { ExportDrawingServiceMock } from './export-drawing-service-mock';

describe('ExportDrawingServiceMock', () => {
    it('should create an instance', () => {
        expect(new ExportDrawingServiceMock()).toBeTruthy();
    });
});
